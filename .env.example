# QuantumHive Voice Engine — plantilla de entorno
#
# Copiar a .env y completar. El archivo .env está en .gitignore y NUNCA se sube:
# este repositorio es PÚBLICO.
#
# Ninguna de estas claves puede llegar al navegador. El frontend recibe
# únicamente un token temporal de LiveKit emitido por nuestro backend.

# ── LiveKit — SELF-HOSTED (no usamos LiveKit Cloud) ────────
# Desarrollo:  livekit-server --dev en la máquina local.
#              URL ws://localhost:7880 y las credenciales de dev.
# Producción:  el mismo binario en un VPS propio, con TLS (wss://).
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# ── PLAN COMERCIAL — que motor de conversacion usa este tenant ──
#   pipeline  basico    Groq + Fish. Texto en el medio.   ~USD 0,013/min
#   gemini    medio     Gemini Live, voz a voz.           ~USD 0,012/min
#   openai    premium   OpenAI Realtime, voz a voz.       ~USD 0,016/min (mini)
MOTOR=pipeline

# ── Plan medio · Gemini Live ───────────────────────────────
# Con GCP_PROJECT va por Vertex AI y consume creditos de Google Cloud.
# Con GOOGLE_API_KEY va por pago por uso, contra tarjeta.
GCP_PROJECT=
GCP_LOCATION=us-east4
GOOGLE_API_KEY=
# OJO: Vertex AI usa nombres de modelo distintos que ai.google.dev.
GEMINI_MODEL=gemini-live-2.5-flash-native-audio
GEMINI_VOICE=Puck
GEMINI_TEMPERATURE=0.8

# ── Plan premium · OpenAI Realtime ─────────────────────────
# Con AZURE_OPENAI_ENDPOINT consume creditos de Azure.
# Con OPENAI_API_KEY va por pago por uso, contra tarjeta.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_KEY=
OPENAI_API_KEY=
OPENAI_REALTIME_MODEL=gpt-realtime-2.1-mini
OPENAI_VOICE=alloy

# ── Groq — STT y LLM ───────────────────────────────────────
GROQ_API_KEY=
GROQ_STT_MODEL=whisper-large-v3-turbo
GROQ_LLM_MODEL=openai/gpt-oss-20b
# Pista de vocabulario para Whisper. Vacio = sin pista.
# Sin esto la marca se transcribe como "quantum high".
STT_PROMPT=

# ── Fish Audio — TTS ───────────────────────────────────────
FISH_API_KEY=
# s2.1-pro es el pago: devuelve HTTP 402 si la cuenta no tiene credito.
# s2.1-pro-free es gratuito y funciona sin credito, hasta el 31/08/2026.
FISH_MODEL=s2.1-pro
FISH_LATENCY_MODE=low
# Ritmo del habla. A 1.0 suena pausado y robotico entre palabras.
# 1.12 validado a oido como el ritmo natural.
FISH_SPEED=1.25
# Expresividad. Mas alto = mas variado, mas bajo = mas estable.
FISH_TEMPERATURE=1.0
FISH_VOICE_ID=
IDIOMA=es

# ── Supabase — control plane ───────────────────────────────
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# ── QuantumCore → CEO departamental ───────────────────────
# Token maquina-a-maquina exclusivo para las rutas /v1 del CEO. Si queda
# vacio, esas rutas fallan cerradas. No reutilizar claves de proveedores.
QUANTUMCORE_TOKEN=

# ── Perfilador de Clientes → Fábrica de Agentes ────────────
# Conexión privada al repo INTELIGENCIA-COMERCIAL-SCRAP. La Fábrica llama
# desde su backend; estas variables nunca llevan prefijo VITE_ ni viajan al
# navegador del cliente.
CENTRO_INTELIGENCIA_URL=
CENTRO_INTELIGENCIA_TOKEN=

# ── WhatsApp (Meta Cloud API) ──────────────────────────────
# Estas variables son de la APP de Meta, no de un número. El access token de cada
# número NO va acá: va al almacén de secretos, y tenant_canales guarda solo la
# referencia. Un número se conecta cargando una fila, no editando este archivo.
META_APP_ID=
META_APP_SECRET=
# ID de la configuración "WhatsApp Embedded Signup" creada en Meta.
META_EMBEDDED_SIGNUP_CONFIG_ID=
# Lo inventás vos. Se lo pegás a Meta al dar de alta el webhook y tiene que
# coincidir. Si queda vacío, el webhook rechaza toda verificación.
WHATSAPP_VERIFY_TOKEN=
# PIN de seis dígitos elegido por QuantumHive para registrar los números.
WHATSAPP_REGISTRATION_PIN=
# Fijada explícita. "la última" cambia sola y rompe sin aviso.
WHATSAPP_API_VERSION=v21.0

# Carpeta privada donde el alta automática guarda los tokens de cada número.
# En Linux de producción debe ser absoluta y persistente, por ejemplo:
# WHATSAPP_SECRET_DIR=/var/lib/quantumhive/secretos

# Compatibilidad con canales cargados a mano: el token también puede venir de
# una variable. `tenant_canales` guarda solo la referencia, que se resuelve así:
#   secreto_ref = "whatsapp_quantumhive"  →  SECRETO_WHATSAPP_QUANTUMHIVE
# Una línea por canal conectado. Ver src/motor_voz/channels/secretos.py.
# SECRETO_WHATSAPP_QUANTUMHIVE=

# ── Límites de gasto y abuso ───────────────────────────────
# El botón rojo. En `off` el agente deja de contestar en TODOS los canales y
# todos los tenants, sin desconectar nada: los mensajes siguen entrando y
# quedan en la conversación. Los topes por negocio van en `tenant_limites`.
RESPUESTAS_AUTOMATICAS=on
MAX_SESSION_SECONDS=240
MAX_SESSIONS_PER_IP_HOUR=20
MAX_SESSIONS_PER_DAY=300
API_PUERTO=8080

# ── Aplicación ─────────────────────────────────────────────
ENVIRONMENT=development
LOG_LEVEL=INFO
